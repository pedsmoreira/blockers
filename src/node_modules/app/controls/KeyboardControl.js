// @flow

import Mousetrap from "mousetrap";
import Control from "./Control";
import * as keyboardControlKeys from "app/constants/keyboardControlKeys";

export default class KeyboardControl extends Control {
    setup() {
        Mousetrap.bind(keyboardControlKeys.DECAY, () => this.actions.move(0, 1));
        Mousetrap.bind(keyboardControlKeys.DROP, () => this.actions.drop());
        Mousetrap.bind(keyboardControlKeys.ROTATE, () => this.actions.rotate());
        Mousetrap.bind(keyboardControlKeys.LEFT, () => this.actions.move(-1, 0));
        Mousetrap.bind(keyboardControlKeys.RIGHT, () => this.actions.move(1, 0));

        // Mousetrap.bind(keyboardControlKeys.SPELL_0, () => this.cast(0));
        // Mousetrap.bind(keyboardControlKeys.SPELL_1, () => this.cast(1));
        // Mousetrap.bind(keyboardControlKeys.SPELL_2, () => this.cast(2));
    }
}
