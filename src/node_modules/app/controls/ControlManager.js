// @flow

import type {ControlActions} from "app/controls/Control";
import Control from "app/controls/Control";

export default class ControlManager {
    controls: Control[];

    constructor(actions: ControlActions, controls: Function[]) {
        this.controls = controls.map((control: Function) => new control(actions));
    }

    reset() {
    }
}
