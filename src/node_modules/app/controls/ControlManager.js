import Control from "app/controls/Control";
import type ControlCallbacks from "app/controls/Control";

export default class ControlManager {
    controls: Control[];

    constructor(callbacks: ControlCallbacks, controls: Function[]) {
        this.controls = controls.map((control: Function) => new control(callbacks));
    }

    reset() {

    }
}
