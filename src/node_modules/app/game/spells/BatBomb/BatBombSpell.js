// @flow

import Spell from "app/models/Spell";

import icon from "./bat-bomb-icon.png"

export default class BatBombSpell extends Spell {
    static title = 'Bat Bomb';
    static castDescription: string;
    static castable: boolean = true;

    static icon = icon;

    cast() {
    }
}
