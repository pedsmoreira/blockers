// @flow

import Spell from "app/models/Spell";

import icon from "./bat-bomb-icon.jpg"

export default class BatBombSpell extends Spell {
    static title = 'Bat Bomb';
    static castDescription = 'Turn the piece into a bomb that explodes nearby blocks';

    static castable = true;

    static icon = icon;

    cast() {
    }
}
