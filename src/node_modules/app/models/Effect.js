// @flow

import {observable} from "mobx";
import type {Position} from "app/models/Matrix";

export default class Effect {
    @observable position: Position;
    id: number;

    static _id = 1;

    constructor() {
        this.id = Effect._id++;
    }

    render(blockSize: number) {
        throw new Error('Undefined method render for Effect');
    }
}
