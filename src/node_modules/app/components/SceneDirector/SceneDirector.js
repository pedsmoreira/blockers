// @flow

import React, {Component} from "react";
import {observer} from "mobx-react";
import "./SceneDirector.scss";

@observer
export default class SceneDirector extends Component {
    render() {
        return (
            <div className="SceneDirector">
                {this.props.scene}
            </div>
        )
    }
}
