// @flow

import {observer} from "mobx-react";
import React from "react";
import FontAwesome from "react-fontawesome";

import SpellModel from "app/models/Spell";

import Spell from "app/components/Spell/Spell";

import "./SpellsMenu.scss";

@observer
export default class SpellsMenu extends React.PureComponent {
    props: {
        spells: Spell[],
        castKeys: string[]
    };

    render() {
        const spells = this.props.spells;
        const castKeys = this.props.castKeys;

        return (
            <div className="SpellsMenu">
                <header>
                    <h2>
                        <FontAwesome name="magic"/> Spells
                    </h2>
                </header>

                <div className="SpellsMenu-spells">
                    {spells.map((spell: SpellModel, index: number) => {
                        return <Spell key={spell.constructor.title}
                                      spell={spell}
                                      castKey={castKeys[index]}
                        />;
                    })}
                </div>
            </div>
        )
    }
}
