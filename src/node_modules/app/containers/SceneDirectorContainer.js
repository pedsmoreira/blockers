// @flow

import {observer} from "mobx-react";
import React from "react";

import SceneStore from "app/stores/SceneStore";

import SceneDirector from "app/components/SceneDirector/SceneDirector";

@observer
export default class SceneDirectorContainer extends React.PureComponent {
    render() {
        return (
            <SceneDirector scene={SceneStore.scene} modalScene={SceneStore.modalScene}/>
        );
    }
}
